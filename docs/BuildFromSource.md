# Build OverEngine from sourcecode

Windows (Python3):
```
python build.py -b
```

for Windows, make sure you added `msbuild` command's directory into the `path` enviorment variable

Linux :
```
python3 build.py -b
```

for Linux, make sure you installed `make`